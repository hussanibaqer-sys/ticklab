diff -ru '--exclude=obj' '--exclude=bin' /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/Controls/TickChartControl.cs /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/Controls/TickChartControl.cs
--- /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/Controls/TickChartControl.cs	2026-08-15 18:53:21.000000000 +0000
+++ /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/Controls/TickChartControl.cs	2026-08-15 19:24:11.288264832 +0000
@@ -18,9 +18,9 @@
     private const double DefaultRightBlankSpace = 288.0;
     private const int MinimumVisibleTicks = 1;
     // User-facing raw Tick Chart safety ceiling: never squeeze more than
-    // 1,600 individual ticks into the visible chart frame. History remains
+    // 5,000 individual ticks into the visible chart frame. History remains
     // available through normal horizontal panning/paging.
-    private const int MaximumVisibleTicks = 1600;
+    private const int MaximumVisibleTicks = 5000;
     private const double WheelZoomStep = 1.08;
     private const double MaximumZoomFactor = 100.0;
 
@@ -107,6 +107,9 @@
     {
         get
         {
+            if (IsHistoricalWindow)
+                return false;
+
             int count = EffectiveTickCount;
             if (count == 0)
                 return true;
@@ -118,9 +121,16 @@
 
     public event EventHandler? ViewChanged;
     public event EventHandler? OlderHistoryRequested;
+    public event EventHandler? GoToLatestRequested;
 
     public bool CanRequestOlderHistory { get; set; }
 
+    // A direct historical Tick Find is not the live edge even if the target
+    // happens to sit at the end of the currently loaded local window. This
+    // keeps the live right-side breathing space hidden and keeps the Live/End
+    // action available until MainWindow reloads the true latest raw ticks.
+    public bool IsHistoricalWindow { get; set; }
+
     public int FirstVisibleIndex
     {
         get
@@ -314,6 +324,47 @@
         RaiseViewChanged();
     }
 
+    public bool ShowTimestamp(long timeMilliseconds, int visibleCount = 1600)
+    {
+        int count = EffectiveTickCount;
+        if (count <= 0)
+            return false;
+
+        int low = 0;
+        int high = count;
+        while (low < high)
+        {
+            int middle = low + (high - low) / 2;
+            if (_ticks[middle].TimeMilliseconds < timeMilliseconds)
+                low = middle + 1;
+            else
+                high = middle;
+        }
+
+        int index = Math.Clamp(low, 0, count - 1);
+        if (index > 0 &&
+            Math.Abs(_ticks[index - 1].TimeMilliseconds - timeMilliseconds) <=
+            Math.Abs(_ticks[index].TimeMilliseconds - timeMilliseconds))
+        {
+            index--;
+        }
+
+        int wanted = Math.Clamp(
+            visibleCount,
+            GetMinimumVisibleCount(count),
+            GetMaximumVisibleCount(count));
+        _horizontalAuto = false;
+        _viewCount = wanted;
+        _viewStart = Math.Clamp(
+            index - wanted / 2,
+            0,
+            Math.Max(0, count - wanted));
+        _verticalAuto = true;
+        InvalidateVisual();
+        RaiseViewChanged();
+        return true;
+    }
+
     public void GoToLatest()
     {
         int count = EffectiveTickCount;
@@ -387,7 +438,10 @@
         base.OnKeyDown(e);
         if (e.Key == Key.End)
         {
-            GoToLatest();
+            if (GoToLatestRequested is not null)
+                GoToLatestRequested.Invoke(this, EventArgs.Empty);
+            else
+                GoToLatest();
             e.Handled = true;
             return;
         }
@@ -448,7 +502,10 @@
 
         if (!IsAtLatest && GetLatestButtonRect().Contains(mouse))
         {
-            GoToLatest();
+            if (GoToLatestRequested is not null)
+                GoToLatestRequested.Invoke(this, EventArgs.Empty);
+            else
+                GoToLatest();
             e.Handled = true;
             return;
         }
diff -ru '--exclude=obj' '--exclude=bin' /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.ChartContexts.cs /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.ChartContexts.cs
--- /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.ChartContexts.cs	2026-08-15 18:27:38.000000000 +0000
+++ /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.ChartContexts.cs	2026-08-15 19:24:27.947200120 +0000
@@ -7,6 +7,7 @@
 using TickLab.Core.Scripting;
 using TickLab.Core.Settings;
 using TickLab.Desktop.Controls;
+using TickLab.Desktop.Windows;
 
 namespace TickLab.Desktop;
 
@@ -591,7 +592,16 @@
         // A Find Candle jump creates a persistent historical navigation anchor.
         // Changing candle timeframe must stay around that same market moment
         // instead of resetting the pane to the latest/live candle.
-        if (historicalAnchor.HasValue && !timeframe.IsRawTickChart)
+        if (historicalAnchor.HasValue && timeframe.IsRawTickChart)
+        {
+            await FindRawTickAsync(new MarkerDraft(
+                symbol,
+                "Tick",
+                historicalAnchor.Value,
+                string.Empty,
+                historicalAnchor.Value));
+        }
+        else if (historicalAnchor.HasValue)
         {
             await CenterActiveChartOnHistoricalTimestampAsync(
                 context,
diff -ru '--exclude=obj' '--exclude=bin' /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.RawTickChart.cs /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.RawTickChart.cs
--- /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.RawTickChart.cs	2026-08-15 18:52:11.000000000 +0000
+++ /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.RawTickChart.cs	2026-08-15 19:24:11.288448987 +0000
@@ -1,5 +1,6 @@
 using System.Windows;
 using TickLab.Core.Market;
+using TickLab.Desktop.Windows;
 using TickLab.Gateway.FileBridge;
 
 namespace TickLab.Desktop;
@@ -13,6 +14,22 @@
     {
         context.TickChart.Settings = context.Settings;
         context.TickChart.PreviewMouseLeftButtonDown += (_, _) => ActivateWorkspacePane(context.PaneId);
+        context.TickChart.GoToLatestRequested += async (_, _) =>
+        {
+            if (context.HistoricalNavigationAnchorUnix.HasValue)
+            {
+                context.HistoricalNavigationAnchorUnix = null;
+                context.HistoricalNavigationAnchorSymbol = string.Empty;
+                context.Chart.HistoricalNavigationAnchorUnix = null;
+                context.TickChart.IsHistoricalWindow = false;
+                await LoadRawTickChartAsync(context, resetViewport: true, cancellationToken: _lifetime.Token);
+                await RefreshRawTickChartLiveAsync(context, force: true);
+            }
+            else
+            {
+                context.TickChart.GoToLatest();
+            }
+        };
         context.TickChart.OlderHistoryRequested += async (_, _) =>
         {
             try
@@ -138,6 +155,8 @@
         context.TickAllOlderLoaded = ticks.Length == 0 || ticks[0].TimeMilliseconds <= coverage.EarliestTimeMilliseconds;
         context.TickChart.CanRequestOlderHistory = false;
         context.TickChart.Settings = context.Settings;
+        if (!context.HistoricalNavigationAnchorUnix.HasValue)
+            context.TickChart.IsHistoricalWindow = false;
         context.TickChart.Ticks = context.TickHistory;
         if (resetViewport && ticks.Length > 0)
             context.TickChart.ShowLatest(Math.Min(RawTickInitialVisibleCount, ticks.Length));
@@ -235,10 +254,133 @@
         }
     }
 
+    private async Task FindRawTickAsync(MarkerDraft draft)
+    {
+        Mt5ConnectorSummary? selectedConnector = _selectedConnector;
+        if (selectedConnector is null)
+        {
+            _markerWindow?.SetStatus("No MT5/history connection is selected for Tick Find.");
+            return;
+        }
+
+        TimeframeDefinition tickTimeframe =
+            TimeframeDefinition.FindBuiltIn(1, TimeframeUnit.Tick)!;
+        string symbol = draft.Symbol.Trim();
+        long requestedUnix = draft.RequestedUnix ?? draft.StartUnix;
+        long requestedMilliseconds = checked(requestedUnix * 1000L);
+
+        if (!ActiveChartContext.Timeframe.IsRawTickChart ||
+            !string.Equals(ActiveChartContext.Symbol, symbol, StringComparison.OrdinalIgnoreCase))
+        {
+            await SafeSelectChartAsync(symbol, tickTimeframe);
+        }
+
+        ChartRuntimeContext context = ActiveChartContext;
+        if (!context.Timeframe.IsRawTickChart ||
+            !string.Equals(context.Symbol, symbol, StringComparison.OrdinalIgnoreCase))
+        {
+            _markerWindow?.SetStatus("Tick Find could not open the requested Raw Tick chart.");
+            return;
+        }
+
+        CanonicalTickCoverage coverage = await Task.Run(
+            () => _historyStore.GetTickCoverageForReplay(
+                selectedConnector.ConnectorId,
+                symbol,
+                _lifetime.Token),
+            CancellationToken.None);
+
+        long halfWindow = 60_000L;
+        MarketTick[] ticks = Array.Empty<MarketTick>();
+        for (int attempt = 0; attempt < 4 && ticks.Length == 0; attempt++)
+        {
+            long sourceStart = Math.Max(0, requestedMilliseconds - halfWindow);
+            long sourceEnd = requestedMilliseconds + halfWindow + 1000L;
+            if (sourceEnd <= sourceStart)
+                break;
+
+            CanonicalTickReadResult direct = await Task.Run(
+                () => _historyStore.ReadBridgeTicksForReplayFast(
+                    selectedConnector.ConnectorId,
+                    symbol,
+                    sourceStart,
+                    sourceEnd,
+                    maximumRecords: RawTickReadLimit,
+                    serverUtcOffsetMinutes: selectedConnector.ServerUtcOffsetMinutes,
+                    cancellationToken: _lifetime.Token),
+                CancellationToken.None);
+
+            if (direct.Ticks.Count == 0 && coverage.HasData)
+            {
+                long archiveStart = Math.Max(coverage.EarliestTimeMilliseconds, sourceStart);
+                long archiveEnd = Math.Min(coverage.LatestTimeMilliseconds + 1, sourceEnd);
+                if (archiveEnd > archiveStart)
+                {
+                    direct = await Task.Run(
+                        () => _historyStore.ReadTicksForReplay(
+                            selectedConnector.ConnectorId,
+                            symbol,
+                            archiveStart,
+                            archiveEnd,
+                            RawTickReadLimit,
+                            _lifetime.Token),
+                        CancellationToken.None);
+                }
+            }
+
+            ticks = direct.Ticks
+                .OrderBy(item => item.TimeMilliseconds)
+                .ToArray();
+            halfWindow *= 5;
+        }
+
+        if (ticks.Length == 0)
+        {
+            _markerWindow?.SetStatus(
+                "No stored raw tick was found around that server date/time. Refresh Tick Data if MT5 still provides that range.");
+            return;
+        }
+
+        MarketTick found = ticks
+            .Where(item => item.TimeMilliseconds >= requestedMilliseconds &&
+                           item.TimeMilliseconds < requestedMilliseconds + 1000L)
+            .FirstOrDefault();
+        if (found == default)
+        {
+            found = ticks.MinBy(item => Math.Abs(item.TimeMilliseconds - requestedMilliseconds));
+        }
+
+        context.TickHistory = ticks.ToList();
+        context.LastRawTickMilliseconds = ticks[^1].TimeMilliseconds;
+        context.TickAllOlderLoaded = coverage.HasData &&
+            ticks[0].TimeMilliseconds <= coverage.EarliestTimeMilliseconds;
+        context.TickChart.Settings = context.Settings;
+        context.TickChart.Ticks = context.TickHistory;
+        context.TickChart.CanRequestOlderHistory = !context.TickAllOlderLoaded;
+        context.HistoricalNavigationAnchorUnix = requestedUnix;
+        context.HistoricalNavigationAnchorSymbol = symbol;
+        context.Chart.HistoricalNavigationAnchorUnix = requestedUnix;
+        context.TickChart.IsHistoricalWindow = true;
+        context.TickChart.ShowTimestamp(
+            found.TimeMilliseconds,
+            Math.Min(RawTickInitialVisibleCount, ticks.Length));
+        SetRawTickMode(context, true);
+        CandleCountText.Text = context.TickHistory.Count.ToString("N0");
+
+        string foundTime = DateTimeOffset
+            .FromUnixTimeMilliseconds(found.TimeMilliseconds)
+            .ToUniversalTime()
+            .ToString("yyyy-MM-dd HH:mm:ss.fff", System.Globalization.CultureInfo.InvariantCulture);
+        _markerWindow?.SetStatus($"Tick found and centered: {symbol} {foundTime} server time.");
+        StatusText.Text = $"Tick Find: {symbol} {foundTime} · historical tick window.";
+    }
+
     private async Task RefreshRawTickChartLiveAsync(ChartRuntimeContext context, bool force = false)
     {
         if (_selectedConnector is null || !context.Timeframe.IsRawTickChart || string.IsNullOrWhiteSpace(context.Symbol))
             return;
+        if (context.HistoricalNavigationAnchorUnix.HasValue)
+            return;
         DateTime now = DateTime.UtcNow;
         if (!force && now - context.LastRawTickRefreshUtc < TimeSpan.FromMilliseconds(175))
             return;
diff -ru '--exclude=obj' '--exclude=bin' /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.xaml /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.xaml
--- /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.xaml	2026-08-15 18:52:11.000000000 +0000
+++ /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.xaml	2026-08-15 19:22:57.045184228 +0000
@@ -3,7 +3,7 @@
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:controls="clr-namespace:TickLab.Desktop.Controls"
         xmlns:settings="clr-namespace:TickLab.Desktop.Settings"
-        Title="TickLab v1.13.0.140 — Find Loading + Tick Chart Navigation"
+        Title="TickLab v1.13.0.141 — Tick Find / Find Contrast / 5000 Tick Zoom"
         Width="1560" Height="920" MinWidth="1080" MinHeight="690"
         WindowStartupLocation="CenterScreen"
         Background="{StaticResource WindowBrush}"
diff -ru '--exclude=obj' '--exclude=bin' /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.xaml.cs /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.xaml.cs
--- /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.xaml.cs	2026-08-15 18:52:11.000000000 +0000
+++ /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/MainWindow.xaml.cs	2026-08-15 19:24:58.531023325 +0000
@@ -731,15 +731,23 @@
     {
         if (_findCandleInProgress)
         {
-            _markerWindow?.SetStatus("Find Candle is already finding a candle. Finish the current search before starting another.");
+            _markerWindow?.SetStatus("Find is already running. Finish the current candle/tick search before starting another.");
             return;
         }
 
         _findCandleInProgress = true;
         _markerWindow?.SetFindingState(true);
-        _markerWindow?.SetStatus("Finding candle…");
+        _markerWindow?.SetStatus(string.Equals(draft.Timeframe, "Tick", StringComparison.OrdinalIgnoreCase)
+            ? "Finding tick…"
+            : "Finding candle…");
         try
         {
+            if (string.Equals(draft.Timeframe, "Tick", StringComparison.OrdinalIgnoreCase))
+            {
+                await FindRawTickAsync(draft);
+                return;
+            }
+
             await CreateMarkerAsync(draft, sendToMt5: false);
             CandleMarker? marker = _candleMarkers.FirstOrDefault(item =>
                 string.Equals(item.Symbol, draft.Symbol, StringComparison.OrdinalIgnoreCase) &&
diff -ru '--exclude=obj' '--exclude=bin' /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/TickLab.App.csproj /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/TickLab.App.csproj
--- /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/TickLab.App.csproj	2026-08-15 18:52:11.000000000 +0000
+++ /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/TickLab.App.csproj	2026-08-15 19:21:25.164187206 +0000
@@ -8,9 +8,9 @@
     <LangVersion>12.0</LangVersion>
     <AssemblyName>TickLab</AssemblyName>
     <RootNamespace>TickLab.Desktop</RootNamespace>
-    <Version>1.13.0.140</Version>
-    <AssemblyVersion>1.13.0.140</AssemblyVersion>
-    <FileVersion>1.13.0.140</FileVersion>
+    <Version>1.13.0.141</Version>
+    <AssemblyVersion>1.13.0.141</AssemblyVersion>
+    <FileVersion>1.13.0.141</FileVersion>
     <RestoreIgnoreFailedSources>true</RestoreIgnoreFailedSources>
     <Deterministic>true</Deterministic>
   </PropertyGroup>
diff -ru '--exclude=obj' '--exclude=bin' /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/Windows/CandleMarkerWindow.xaml /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/Windows/CandleMarkerWindow.xaml
--- /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/Windows/CandleMarkerWindow.xaml	2026-08-15 18:52:11.000000000 +0000
+++ /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/Windows/CandleMarkerWindow.xaml	2026-08-15 19:22:57.015409787 +0000
@@ -1,6 +1,7 @@
 <Window x:Class="TickLab.Desktop.Windows.CandleMarkerWindow"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
+        xmlns:settings="clr-namespace:TickLab.Desktop.Settings"
         Title="Candle Markers — MT5 ↔ TickLab"
         Width="700" Height="660" MinWidth="640" MinHeight="580"
         WindowStartupLocation="CenterOwner" Background="#0B0B0B"
@@ -25,6 +26,43 @@
             <Setter Property="Background" Value="White"/>
             <Setter Property="Foreground" Value="Black"/>
         </Style>
+
+        <Style x:Key="FindActionButtonStyle" TargetType="{x:Type Button}">
+            <Setter Property="Margin" Value="4"/>
+            <Setter Property="Padding" Value="12,7"/>
+            <Setter Property="Background" Value="#7C3AED"/>
+            <Setter Property="Foreground" Value="White"/>
+            <Setter Property="BorderBrush" Value="#A78BFA"/>
+            <Setter Property="BorderThickness" Value="1"/>
+            <Setter Property="Cursor" Value="Hand"/>
+            <Setter Property="Template">
+                <Setter.Value>
+                    <ControlTemplate TargetType="{x:Type Button}">
+                        <Border x:Name="FindButtonBorder" CornerRadius="5"
+                                Background="{TemplateBinding Background}"
+                                BorderBrush="{TemplateBinding BorderBrush}"
+                                BorderThickness="{TemplateBinding BorderThickness}"
+                                Padding="{TemplateBinding Padding}">
+                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
+                        </Border>
+                        <ControlTemplate.Triggers>
+                            <Trigger Property="IsMouseOver" Value="True">
+                                <Setter TargetName="FindButtonBorder" Property="Background" Value="#6D28D9"/>
+                            </Trigger>
+                            <Trigger Property="IsPressed" Value="True">
+                                <Setter TargetName="FindButtonBorder" Property="Background" Value="#5B21B6"/>
+                            </Trigger>
+                            <Trigger Property="IsEnabled" Value="False">
+                                <Setter TargetName="FindButtonBorder" Property="Background" Value="#4C1D95"/>
+                                <Setter TargetName="FindButtonBorder" Property="BorderBrush" Value="#8B5CF6"/>
+                                <Setter Property="Foreground" Value="White"/>
+                                <Setter Property="Cursor" Value="Arrow"/>
+                            </Trigger>
+                        </ControlTemplate.Triggers>
+                    </ControlTemplate>
+                </Setter.Value>
+            </Setter>
+        </Style>
     </Window.Resources>
     <Grid Margin="14">
         <Grid.RowDefinitions>
@@ -79,7 +117,7 @@
             </StackPanel>
             <StackPanel Grid.Column="3">
                 <TextBlock Text="Timeframe" Margin="4,0"/>
-                <ComboBox x:Name="TimeframeBox"/>
+                <ComboBox x:Name="TimeframeBox" SelectionChanged="TimeframeBox_SelectionChanged"/>
             </StackPanel>
         </Grid>
 
@@ -103,20 +141,21 @@
                     <ColumnDefinition Width="Auto"/>
                 </Grid.ColumnDefinitions>
                 <StackPanel>
-                    <TextBlock Text="Manual Find Candle" FontWeight="SemiBold"/>
-                    <TextBlock Text="Uses the symbol, server date, time and timeframe above. It only loads and centres the candle; it never imports or exports history."
+                    <TextBlock Text="Manual Find Candle / Tick" FontWeight="SemiBold"/>
+                    <TextBlock Text="Uses the symbol, server date, time and timeframe above. Tick finds the nearest raw tick in that entered second; candle timeframes load and centre the candle. It never imports history."
                                Foreground="#C4B5FD" TextWrapping="Wrap" Margin="0,3,10,0"/>
                 </StackPanel>
-                <Button x:Name="FindButton" Grid.Column="1" Background="#7C3AED"
-                        Click="FindButton_Click" VerticalAlignment="Center" MinWidth="112">
+                <Button x:Name="FindButton" Grid.Column="1" Style="{StaticResource FindActionButtonStyle}"
+                        settings:ThemeColorScope.PreserveExactColors="True"
+                        Click="FindButton_Click" VerticalAlignment="Center" MinWidth="122">
                     <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
-                        <TextBlock x:Name="FindSpinner" Text="↻" FontSize="17" FontWeight="SemiBold"
+                        <TextBlock x:Name="FindSpinner" Text="↻" Foreground="White" FontSize="17" FontWeight="SemiBold"
                                    Visibility="Collapsed" Margin="0,0,6,0" RenderTransformOrigin="0.5,0.5">
                             <TextBlock.RenderTransform>
                                 <RotateTransform x:Name="FindSpinnerRotate" Angle="0"/>
                             </TextBlock.RenderTransform>
                         </TextBlock>
-                        <TextBlock x:Name="FindButtonText" Text="Find candle" VerticalAlignment="Center"/>
+                        <TextBlock x:Name="FindButtonText" Text="Find candle" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center"/>
                     </StackPanel>
                 </Button>
             </Grid>
diff -ru '--exclude=obj' '--exclude=bin' /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/Windows/CandleMarkerWindow.xaml.cs /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/Windows/CandleMarkerWindow.xaml.cs
--- /mnt/data/ticklab_v140_orig/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/Windows/CandleMarkerWindow.xaml.cs	2026-08-15 18:53:00.000000000 +0000
+++ /mnt/data/ticklab_v141/TickLabV1_13_0_140_Find_Loading_TickChart_Navigation/src/TickLab.App/Windows/CandleMarkerWindow.xaml.cs	2026-08-15 19:22:57.016085796 +0000
@@ -15,7 +15,7 @@
     private IReadOnlyList<string> _symbolOptions = Array.Empty<string>();
     private static readonly IReadOnlyList<string> FindCandleTimeframes = new[]
     {
-        "1s", "15s", "30s", "45s"
+        "Tick", "1s", "15s", "30s", "45s"
     }.Concat(TimeframeDefinition.NativeMt5Timeframes).ToArray();
     private bool _allowClose;
     private string _lastAutoNavigatedMarkerId = string.Empty;
@@ -108,7 +108,7 @@
     {
         _findInProgress = finding;
         FindButton.IsEnabled = !finding;
-        FindButtonText.Text = finding ? "Finding…" : "Find candle";
+        FindButtonText.Text = finding ? "Finding…" : GetIdleFindButtonText();
         FindSpinner.Visibility = finding ? Visibility.Visible : Visibility.Collapsed;
 
         if (finding)
@@ -129,6 +129,17 @@
         }
     }
 
+    private string GetIdleFindButtonText() =>
+        string.Equals(TimeframeBox.SelectedItem?.ToString(), "Tick", StringComparison.OrdinalIgnoreCase)
+            ? "Find tick"
+            : "Find candle";
+
+    private void TimeframeBox_SelectionChanged(object sender, System.Windows.Controls.SelectionChangedEventArgs e)
+    {
+        if (!_findInProgress && FindButtonText is not null)
+            FindButtonText.Text = GetIdleFindButtonText();
+    }
+
     private void SymbolBox_PreviewMouseLeftButtonDown(object sender, MouseButtonEventArgs e)
     {
         if (_symbolOptions.Count > 0)
@@ -203,6 +214,11 @@
 
         if (!TryReadDraft(out MarkerDraft? draft))
             return;
+        if (string.Equals(draft!.Timeframe, "Tick", StringComparison.OrdinalIgnoreCase))
+        {
+            StatusText.Text = "Tick is a Find-only target. Use Find candle to open and centre the Raw Tick chart.";
+            return;
+        }
         SetMarkMode(true);
         MarkModeChanged?.Invoke(true, draft);
     }
@@ -213,9 +229,9 @@
             return;
         MarkerDraft exportDraft = draft!;
         if (TryResolveFindTimeframe(exportDraft.Timeframe, out TimeframeDefinition exportTimeframe) &&
-            exportTimeframe.Unit == TimeframeUnit.Second)
+            exportTimeframe.Unit is TimeframeUnit.Tick or TimeframeUnit.Second)
         {
-            StatusText.Text = "Second timeframes are TickLab-local. Find/Mark works here; MT5 export requires a native MT5 timeframe.";
+            StatusText.Text = "Tick/second timeframes are TickLab-local. Tick Find and second Find/Mark work here; MT5 export requires a native MT5 candle timeframe.";
             return;
         }
         ExportRequested?.Invoke(exportDraft);
@@ -303,6 +319,16 @@
             return false;
 
         string value = raw.Trim();
+        if (string.Equals(value, "Tick", StringComparison.OrdinalIgnoreCase))
+        {
+            TimeframeDefinition? tick = TimeframeDefinition.FindBuiltIn(1, TimeframeUnit.Tick);
+            if (tick is not null)
+            {
+                timeframe = tick;
+                return true;
+            }
+        }
+
         TimeframeDefinition? seconds = TimeframeDefinition.BuiltIns.FirstOrDefault(item =>
             item.Unit == TimeframeUnit.Second &&
             string.Equals(item.DisplayText, value, StringComparison.OrdinalIgnoreCase));
